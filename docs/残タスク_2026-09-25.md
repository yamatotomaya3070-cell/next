# 残タスク（2026-09-25 時点）

本番の最新コミットは `ec25bd0`。提出後AI検品は段階1〜5まで本番反映済みで、この開発機での通し確認（提出→自動照合→差し戻し）は成功している。

## 1. 運用を始めるために必要なこと（最優先）

これが済まないと、作ったものは実際には動かない。動画生成ワーカーも検品ワーカーも、常駐PCが1台動いていることが前提。

| # | 内容 | メモ |
|---|---|---|
| 1-1 | ワーカーを常駐させるPCを決める | 事業所で1台だけ。常時電源が入っているデスクトップ（Windows 10/11）。持ち運ぶノートは不可 |
| 1-2 | そのPCで「セットアップ（職員用）.bat」を実行 | 教科書 第11章からZIPをダウンロード。開発担当が「設定ファイル.txt」（Supabase URL / service role key / Gemini APIキー）を渡す |
| 1-3 | 常駐PCで通し確認 | 利用者が提出 → 数分で「提出物レビュー」の自動照合に結果 → NGがあれば自動で差し戻し。この開発機では確認済みだが、常駐PCでは未確認 |
| 1-4 | すでにセットアップ済みのPCがある場合は「更新（職員用）.bat」 | 9/23 に push した検品ワーカーの修正（自動差し戻し漏れ）を反映するために必要 |

## 2. 提出後AI検品の残り

| # | 内容 | メモ |
|---|---|---|
| 2-1 | 相続税2件のパッケージで精度計測 | `npx tsx scripts/video/measure-inspection-accuracy.ts poc/scene/out/job_0837b376-.../package`（各75分ほど。メモリ不足で止まったら同じコマンドで再開、`--variants` で分割可） |
| 2-2 | 本番のテスト提出を消すか決める | 利用者 花子の新NISA割当 `aaed5b45` に version 1・2 が残置。消す場合は submissions / feedback / submission_inspections / progress_logs の行と Storage の2ファイルを削除し、割当を `in_progress` に戻す |
| 2-3 | （改善案）新形式の見本での映像照合 | 会話場面の見た目がほぼ同じなので、場面差し替え・オープニング欠落を「職員確認」止まりにしか出せない。字幕と音声の照合が補っており差し戻し判定は正しいが、映像でも見分けたいなら別の特徴量が要る。閾値を下げるのは誤検知の危険があるので不可 |

## 3. 動画生成ワーカーの残課題

| # | 内容 | メモ |
|---|---|---|
| 3-1 | 多重起動対策 | 2台で動かすと同じジョブを取り合う。claim 条件に `locked_by is null` を足し、stale なら引き継ぐ |
| 3-2 | シーン並列化 | 現在1件25〜40分。描画が1コア占有の逐次処理 |

## 4. 以前からの残課題（9/18 に挙げた分）

| # | 内容 |
|---|---|
| 4-1 | 支給ZIPの「はじめに.txt」の文面 |
| 4-2 | 教科書6章テロップの「補足」の文言 |
| 4-3 | DaVinci プリセットXMLの初期値 |
| 4-4 | Astra6 指示書の未対応2件: 教材のバージョン管理、教材進捗の職員確認 |
| 4-5 | 要件定義書v2 フェーズ3（メール自動取り込み）・フェーズ4（LINE連携）は未実装。フェーズ2（アプリ内エディタ）は 7/20 にスコープ外と決定 |

## 5. 確認が必要なもの

- 作業ツリーに職員ダッシュボードの未コミット変更が残っている（9/21 16時台、`src/app/staff/page.tsx` の分割、`ActionStrip.tsx` / `TodoList.tsx` / `TraineeRoster.tsx` / `reports/page.tsx` / `src/lib/staff/`）。これは別の作業者のもので、こちらでは触っていない。
- 注意: この作業ツリーは共用で、index に他人の変更が staged のまま残っていることがある。コミット前に `git diff --cached --stat` を見るか、`git commit -- <paths>` でパスを限定する（9/22 にこれが原因で本番ビルドが落ちた）。
